function hybridBestFrameApp(){return{referenceId:'test-person-01',sessionId:null,running:false,capturing:false,busy:false,bestFrame:null,bestScore:0,currentScore:null,quality:{face:'Waiting',size:'-',sharpness:'-',brightness:'-',pose:'-'},liveness:'Waiting',status:'IDLE',result:null,events:[],sampleTimer:null,async start(){if(this.running)return;const video=document.getElementById('preview');if(!video||!video.srcObject){this.status='CAMERA_REQUIRED';this.log('Start the camera before frame analysis','warning');return}if(!this.referenceId.trim()){this.status='REFERENCE_REQUIRED';this.log('Enter a reference ID first','warning');return}this.running=true;this.status='CREATING_SESSION';this.events=[];this.bestFrame=null;this.bestScore=0;this.currentScore=null;this.result=null;try{const r=await fetch('/api/v1/simulator/hybrid/sessions?referenceId='+encodeURIComponent(this.referenceId),{method:'POST'});if(!r.ok)throw new Error('HTTP '+r.status);const data=await r.json();this.sessionId=data.sessionId;this.sampleTimer=setInterval(()=>this.sample(),350);this.status='ANALYZING';this.log('Hybrid capture session started','info')}catch(e){this.running=false;this.status='ERROR';this.log('Could not start hybrid session','error');console.error('[HYBRID]',e)}},async stop(){this.running=false;if(this.sampleTimer)clearInterval(this.sampleTimer);this.sampleTimer=null;this.status=this.bestFrame?'FRAME_SELECTED':'STOPPED';this.log(this.bestFrame?'Best frame retained; session will expire server-side':'Frame selection stopped','info')},async sample(){if(!this.running||this.busy||!this.sessionId)return;const video=document.getElementById('preview');if(!video||!video.videoWidth)return;this.busy=true;try{const canvas=document.createElement('canvas');canvas.width=video.videoWidth;canvas.height=video.videoHeight;canvas.getContext('2d').drawImage(video,0,0);const blob=await new Promise(r=>canvas.toBlob(r,'image/jpeg',.82));if(!blob)throw new Error('Could not encode frame');const fd=new FormData();fd.append('sessionId',this.sessionId);fd.append('image',blob,'hybrid-sample.jpg');const response=await fetch('/api/v1/simulator/frames',{method:'POST',body:fd});if(!response.ok)throw new Error('Frame analysis HTTP '+response.status);const data=await response.json();const score=Number(data.qualityScore||0);const detection=data.detection||{};const live=data.liveness||{};this.currentScore=score;this.quality.face=detection.faceDetected?'Detected':'No face';this.quality.size=detection.box?Math.round(Math.min(detection.box.width,detection.box.height))+' px':'-';this.quality.sharpness=data.quality?.sharpness??data.sharpness??'-';this.quality.brightness=data.quality?.brightness??data.brightness??'-';this.quality.pose=data.quality?.pose??data.pose??'-';this.liveness=live.status||live.frameLooksLive?'LIVE':'Checking';if(detection.faceDetected&&score>=this.bestScore){this.bestScore=score;this.bestFrame=await blobToUrl(blob);this.status='BEST_FRAME_UPDATED';this.log('New best frame selected · score '+score.toFixed(3),'success')}else if(detection.faceDetected){this.status='FACE_TRACKED'}else this.status='WAITING_FOR_FACE'}catch(e){console.error('[HYBRID]',e)}finally{this.busy=false}},async verify(){if(!this.bestFrame||!this.sessionId||this.capturing)return;this.capturing=true;this.running=false;if(this.sampleTimer)clearInterval(this.sampleTimer);this.sampleTimer=null;this.status='UPLOADING_ONE_FRAME';this.result=null;try{const blob=await fetch(this.bestFrame).then(r=>r.blob());const t=performance.now();const response=await fetch('/api/v1/simulator/hybrid/verify?sessionId='+encodeURIComponent(this.sessionId)+'&referenceId='+encodeURIComponent(this.referenceId),{method:'POST',headers:{},body:(()=>{const fd=new FormData();fd.append('image',blob,'best-frame.jpg');return fd})()});const data=await response.json().catch(()=>({result:'PROCESSING_ERROR',reasonCodes:['INVALID_RESPONSE']}));data.clientUploadBytes=blob.size;data.clientRoundTripMs=Math.round(performance.now()-t);this.result=data;this.status=data.result||'COMPLETE';this.log('Server verification completed · '+this.status,data.result==='MATCH'?'success':'info')}catch(e){this.status='SERVER_ERROR';this.result={result:'PROCESSING_ERROR',reasonCodes:['HYBRID_ENDPOINT_ERROR']};this.log('Hybrid server request failed','error');console.error('[HYBRID]',e)}finally{this.capturing=false}},reset(){if(this.sampleTimer)clearInterval(this.sampleTimer);this.sampleTimer=null;if(this.bestFrame)URL.revokeObjectURL(this.bestFrame);this.sessionId=null;this.running=false;this.bestFrame=null;this.bestScore=0;this.currentScore=null;this.result=null;this.status='READY';this.liveness='Waiting';this.quality={face:'Waiting',size:'-',sharpness:'-',brightness:'-',pose:'-'}},log(message,type){this.events.unshift({message,time:new Date().toLocaleTimeString(),type});this.events=this.events.slice(0,8)},formatBytes(n){if(!n)return'0 B';if(n<1024)return n+' B';if(n<1048576)return(n/1024).toFixed(1)+' KB';return(n/1048576).toFixed(2)+' MB'}}}function blobToUrl(blob){return Promise.resolve(URL.createObjectURL(blob))}document.addEventListener('alpine:init',()=>{});